import { injectedClientModule } from '../injectedClientModule.js';
export function previewIframeHistory(env) {
    return {
        name: 'mocha-preview-iframe-history',
        enforce: 'post',
        // See: https://vite.dev/guide/using-plugins#conditional-application
        apply: 'serve',
        ...injectedClientModule('preview-iframe-history', env),
    };
}
