import { injectedClientModule } from '../injectedClientModule.js';
export function mochaWatermark(env) {
    return {
        name: 'mocha-watermark',
        enforce: 'post',
        apply: 'build',
        ...injectedClientModule('mocha-watermark', env, { injectTo: 'body-prepend' }),
    };
}
